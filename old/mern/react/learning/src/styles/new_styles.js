import {css} from 'glamor';

let itemBack = css({
    background: '#03a9f4',
    textAlign: 'center',
    ':hover':{
        color:'red',
        background: 'black'
    }
})

export default itemBack;
