import React, { Component } from 'react';
import itemBack from '../styles/new_styles';

class Footer extends Component {
    render(){
        return(
            <h1 className={ `${itemBack}` }>
                this is the footer
            </h1>
        )
    }
            //<h1 className='logo' {...itemBack}>

}


export default Footer;
